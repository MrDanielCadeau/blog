{
  "private": true,
  "scripts": {
    "build": "nuxt build && cp CNAME dist/ && cp favicon.ico dist/ && cp -r public dist/ && nuxt",
    "dev": "nuxt dev",
    "start": "node .output/server/index.mjs",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "@nuxt/content": "^2.0.1",
    "nuxt": "^3.0.0-rc.8",
    "push-dir": "^0.4.1"
  }
}

{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "start": "node .output/server/index.mjs",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "cp CNAME dist/ && cp favicon.ico dist/ && push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "devDependencies": {
    "@nuxt/content": "^2.0.1",
    "nuxt": "^3.0.0-rc.8",
    "push-dir": "^0.4.1"
  }
}
